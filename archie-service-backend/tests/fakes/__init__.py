"""
Fake repository implementations for testing.

This package contains in-memory fake implementations of repository interfaces
that enable unit testing without external dependencies like databases, Secret
Manager, or external APIs.
"""
